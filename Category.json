{
  "SQLITE": {
    "CREATE_TABLE": [
      "CREATE TABLE Category(",
        "ID   INTEGER   PRIMARY KEY   AUTOINCREMENT,",
        "parent_ID  INTEGER,",
        "name   VARCHAR(255)  NOT NULL,",
        "FOREIGN KEY(parent_ID) REFERENCES MainCategory(ID)",
          "ON UPDATE CASCADE",
          "ON DELETE SET NULL",
      ")"
    ],
    "INSERT": "INSERT INTO Category(name, parent_ID) VALUES(?, ?)",
    "GET": "SELECT ID, name, parent_ID FROM Category WHERE ID = ?",
    "GET_ALL": "SELECT ID, name, parent_ID FROM Category",
    "GET_MAINCAT": "SELECT ID, name, parent_ID FROM Category WHERE parent_ID = ?",
    "UPDATE": "UPDATE Category SET name=?, parent_ID=? WHERE id = ?",
    "DELETE": "DELETE FROM Category WHERE id = ?"
  },
  "MYSQL": {
    "CREATE_TABLE": [
      "CREATE TABLE Category(",
        "ID   int   NOT NULL  AUTO_INCREMENT,",
        "parent_ID  int,",
        "name   VARCHAR(255)  NOT NULL,",
        "PRIMARY KEY(ID),",
        "FOREIGN KEY(parent_ID) REFERENCES MainCategory(ID)",
          "ON UPDATE CASCADE",
          "ON DELETE SET NULL",
      ")"
    ],
    "INSERT": "INSERT INTO Category(name, parent_ID) VALUES(?, ?)",
    "GET": "SELECT ID, name, parent_ID FROM Category WHERE ID = ?",
    "GET_ALL": "SELECT ID, name, parent_ID FROM Category",
    "GET_MAINCAT": "SELECT ID, name, parent_ID FROM Category WHERE parent_ID = ?",
    "UPDATE": "UPDATE Category SET name=?, parent_ID=? WHERE id = ?",
    "DELETE": "DELETE FROM Category WHERE id = ?"
  },
  "POSTGRES": {
    "CREATE_TABLE": [
      "CREATE TABLE Category(",
        "ID   SERIAL  NOT NULL,",
        "parent_ID  int,",
        "name   VARCHAR(255)  NOT NULL,",
        "CONSTRAINT cat_pk PRIMARY KEY(ID),",
        "CONSTRAINT mCat_fk FOREIGN KEY(parent_ID) REFERENCES MainCategory(ID)",
          "ON UPDATE CASCADE",
          "ON DELETE SET NULL",
      ")"
    ],
    "INSERT": "INSERT INTO Category(name, parent_ID) VALUES(?, ?)",
    "GET": "SELECT ID, name, parent_ID FROM Category WHERE ID = ?",
    "GET_ALL": "SELECT ID, name, parent_ID FROM Category",
    "GET_MAINCAT": "SELECT ID, name, parent_ID FROM Category WHERE parent_ID = ?",
    "UPDATE": "UPDATE Category SET name=?, parent_ID=? WHERE id = ?",
    "DELETE": "DELETE FROM Category WHERE id = ?"
  }
}
