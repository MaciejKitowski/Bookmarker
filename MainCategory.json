{
  "SQLITE": {
    "CREATE_TABLE": [
      "CREATE TABLE MainCategory(",
        "ID   INTEGER   PRIMARY KEY AUTOINCREMENT,",
        "name   VARCHAR(255)  NOT NULL",
      ")"
    ],
    "INSERT": "INSERT INTO MainCategory(name) VALUES(?)",
    "GET": "SELECT ID, name FROM MainCategory WHERE id = ?",
    "GET_ALL": "SELECT ID, name FROM MainCategory",
    "UPDATE": "UPDATE MainCategory SET name=? WHERE id = ?",
    "DELETE": "DELETE FROM MainCategory WHERE id = ?"
  },
  "MYSQL": {
    "CREATE_TABLE": [
      "CREATE TABLE MainCategory(",
        "ID   int   NOT NULL  AUTO_INCREMENT,",
        "name   VARCHAR(255)  NOT NULL,",
        "PRIMARY KEY(ID)"
      ")"
    ],
    "INSERT": "INSERT INTO MainCategory(name) VALUES(?)",
    "GET": "SELECT ID, name FROM MainCategory WHERE id = ?",
    "GET_ALL": "SELECT ID, name FROM MainCategory",
    "UPDATE": "UPDATE MainCategory SET name=? WHERE id = ?",
    "DELETE": "DELETE FROM MainCategory WHERE id = ?"
  },
  "POSTGRES": {
    "CREATE_TABLE": [
      "CREATE TABLE MainCategory(",
        "ID   SERIAL  PRIMARY KEY,"
        "name   VARCHAR(255)  NOT NULL"
      ")"
    ],
    "INSERT": "INSERT INTO MainCategory(name) VALUES(?)",
    "GET": "SELECT ID, name FROM MainCategory WHERE id = ?",
    "GET_ALL": "SELECT ID, name FROM MainCategory",
    "UPDATE": "UPDATE MainCategory SET name=? WHERE id = ?",
    "DELETE": "DELETE FROM MainCategory WHERE id = ?"
  }
}
